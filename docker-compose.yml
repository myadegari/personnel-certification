services:
  mongodb:
    image: mongo:latest
    ports:
      - "127.0.0.1:27017:27017"
    environment:
      MONGO_INITDB_ROOT_USERNAME: admin
      MONGO_INITDB_ROOT_PASSWORD: L4run1Cert2025
      MONGO_INITDB_DATABASE: certification
    volumes:
      - mongodb_data:/data/db
    command: --auth
    restart: unless-stopped

volumes:
  mongodb_data:

